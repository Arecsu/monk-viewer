{
  "name": "monk-viewer",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && gh-pages --dotfiles --nojekyll -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~5.7.3",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "@monogrid/gainmap-js": "3.1.0",
    "@types/three": "^0.170.0",
    "js-easing-functions": "^1.0.3",
    "postprocessing": "7.0.0-beta.6",
    "three": "^0.173.0"
  }
}

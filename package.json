{
  "name": "monk-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && gh-pages --dotfiles --nojekyll -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "typescript": "~5.7.2",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "@monogrid/gainmap-js": "file:monogrid-gainmap-js-3.0.6.tgz",
    "@types/three": "^0.170.0",
    "postprocessing": "^6.36.4",
    "three": "^0.171.0"
  }
}
